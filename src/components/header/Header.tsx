import React from "react";
import styles from "./header.module.scss";

const Header: React.FC = () => {
  return <header className={styles.container}>To jest header</header>;
};

export default Header;
